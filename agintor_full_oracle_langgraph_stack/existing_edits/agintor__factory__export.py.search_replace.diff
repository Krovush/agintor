*** FILE: agintor/factory/export.py
*** SEARCH
from ..contracts import DomainEvidenceContract, sealed_benchmark_task_payload
*** REPLACE
from ..contracts import DomainEvidenceContract, sealed_benchmark_task_payload, baseline_langgraph_runtime_spec
from ..oracle.compiler import OracleCompiler
from ..oracle.package_io import write_oracle_package
from ..runtime.langgraph.compiler import RuntimeSpecCompiler
*** SEARCH
def _write_seed_runtime(
    seed_runtime_dir: Path,
    runtime_plan: RuntimePlan,
    *,
    seed_source: Path | None = None,
    runtime_profile: RuntimeProfile | None = None,
    runtime_backend: str | None = None,
) -> None:
*** REPLACE
def _write_seed_runtime(
    seed_runtime_dir: Path,
    runtime_plan: RuntimePlan,
    *,
    seed_source: Path | None = None,
    runtime_profile: RuntimeProfile | None = None,
    runtime_backend: str | None = None,
    runtime_kind: str = "policy_file_v1",
    goal_spec: GoalSpec | None = None,
) -> None:
*** SEARCH
    if seed_source is not None and seed_source.exists() and seed_source.is_dir():
*** REPLACE
    if runtime_kind in {"langgraph_spec_v2", "tradingagents_langgraph_v1"}:
        runtime_spec = baseline_langgraph_runtime_spec(runtime_id=runtime_plan.runtime_id if hasattr(runtime_plan, "runtime_id") else "runtime.langgraph.seed")
        RuntimeSpecCompiler().compile_to_directory(runtime_spec, seed_runtime_dir, force=True)
        if goal_spec is not None:
            oracle_package = OracleCompiler().compile(goal_spec, runtime_spec)
            write_oracle_package(oracle_package, seed_runtime_dir / "oracle")
        return
    if seed_source is not None and seed_source.exists() and seed_source.is_dir():
