*** FILE: agintor/search/archive.py
*** SEARCH
            entry = ArchiveEntry(
                code_hash=code_hash,
                runtime_hash=runtime_hash,
                scores=evaluation.objective_scores,
*** REPLACE
            progress_signal = decision_attr(promotion_decision, "progress_signal")
            mutation_action_ids = [
                str(code).split("mutation_action:", 1)[1]
                for code in (decision_attr(promotion_decision, "reason_codes", []) or [])
                if str(code).startswith("mutation_action:")
            ]
            entry = ArchiveEntry(
                code_hash=code_hash,
                runtime_hash=runtime_hash,
                spec_digest=decision_field_value(promotion_decision, "runtime_spec_digest"),
                parent_spec_digest=decision_field_value(progress_signal, "parent_spec_digest"),
                oracle_package_hash=decision_field_value(promotion_decision, "oracle_package_hash"),
                mutation_action_ids=mutation_action_ids,
                scores=evaluation.objective_scores,
*** SEARCH
            record = ArchiveRecord(
                objective=objective,
                key=cell_key,
                entry=entry,
*** REPLACE
            record = ArchiveRecord(
                objective=objective,
                key=cell_key,
                oracle_package_hash=decision_field_value(promotion_decision, "oracle_package_hash"),
                spec_digest=decision_field_value(promotion_decision, "runtime_spec_digest"),
                entry=entry,
