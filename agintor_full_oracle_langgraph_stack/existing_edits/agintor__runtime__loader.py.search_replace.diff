*** FILE: agintor/runtime/loader.py
*** SEARCH
from ..contracts import CapabilityExchange, DeploymentContract, KernelManifest, RuntimeIsolationPolicy, RuntimeManifest
*** REPLACE
from ..contracts import CapabilityExchange, DeploymentContract, KernelManifest, RuntimeIsolationPolicy, RuntimeManifest
from .langgraph.adapters import build_spec_policy_objects, load_runtime_spec
*** SEARCH
@dataclass
class LoadedRuntime:
    runtime_dir: Path
    manifest: RuntimeManifest
*** REPLACE
@dataclass
class LoadedRuntime:
    runtime_dir: Path
    manifest: RuntimeManifest
    runtime_spec: Any | None = None
*** SEARCH
    policy_objects: Dict[str, Any] = {}
    ast_count = 0
    mutable_loc = 0
    with _runtime_sdk_import_path(runtime_path):
        for key, module_ref in manifest.policy_modules.items():
*** REPLACE
    policy_objects: Dict[str, Any] = {}
    ast_count = 0
    mutable_loc = 0
    runtime_spec = None
    runtime_kind = str(getattr(manifest, "runtime_kind", "") or manifest.metadata.get("runtime_kind", "policy_file_v1"))
    if runtime_kind in {"langgraph_spec_v2", "tradingagents_langgraph_v1"}:
        runtime_spec = load_runtime_spec(runtime_path)
        policy_objects = build_spec_policy_objects(runtime_path)
        ast_count = 0
        mutable_loc = len((runtime_path / "runtime_spec.json").read_text(encoding="utf-8").splitlines())
    else:
        with _runtime_sdk_import_path(runtime_path):
            for key, module_ref in manifest.policy_modules.items():
*** SEARCH
            source = module_path.read_text(encoding="utf-8")
            ast_count += ast_node_count(source)
            mutable_loc += len(source.splitlines())
*** REPLACE
                source = module_path.read_text(encoding="utf-8")
                ast_count += ast_node_count(source)
                mutable_loc += len(source.splitlines())
*** SEARCH
        runtime_dir=runtime_path,
        manifest=manifest,
        kernel_manifest=kernel_manifest,
*** REPLACE
        runtime_dir=runtime_path,
        manifest=manifest,
        runtime_spec=runtime_spec,
        kernel_manifest=kernel_manifest,
