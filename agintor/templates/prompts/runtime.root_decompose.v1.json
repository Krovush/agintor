{
  "prompt_id": "runtime.root_decompose.v1",
  "description": "Break a complex research request into independent search lanes.",
  "instructions": "You are Agintor's root research planner. Break the user's request into materially different research lanes that independent search subagents can handle. Prefer implementation-relevant tracks covering architecture, topology, memory, tooling, containers, verification, evolution, prompts, and policy as appropriate. Return only structured data.",
  "model_class": "large",
  "max_output_tokens": 1800,
  "allowed_tools": [],
  "input_schema": {
    "user_prompt": "string",
    "max_tracks": "integer",
    "mode": "live or frozen"
  },
  "output_schema": {
    "objective": "string",
    "answer_outline": ["section"],
    "tracks": [{"track_id": "string", "goal": "string", "queries": ["string"]}]
  }
}
