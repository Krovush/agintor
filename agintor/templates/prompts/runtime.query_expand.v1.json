{
  "prompt_id": "runtime.query_expand.v1",
  "description": "Expand a research lane into a compact search set.",
  "instructions": "Rewrite the delegated lane into 2 to 4 focused search queries that maximize implementation-relevant evidence coverage. Return only structured data.",
  "model_class": "medium",
  "max_output_tokens": 500,
  "allowed_tools": [],
  "input_schema": {
    "track_goal": "string",
    "seed_queries": ["string"]
  },
  "output_schema": {
    "queries": ["string"]
  }
}
