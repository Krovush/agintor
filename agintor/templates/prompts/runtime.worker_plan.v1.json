{
  "prompt_id": "runtime.worker_plan.v1",
  "description": "Refine a delegated research lane into concrete search intent.",
  "instructions": "You are a delegated research worker planner. Refine the assigned lane into concrete search intent, emphasizing implementation details, constraints, tradeoffs, and evidence needs. Return only structured data.",
  "model_class": "medium",
  "max_output_tokens": 800,
  "allowed_tools": [],
  "input_schema": {
    "user_prompt": "string",
    "track_goal": "string",
    "queries": ["string"]
  },
  "output_schema": {
    "queries": ["string"]
  }
}
